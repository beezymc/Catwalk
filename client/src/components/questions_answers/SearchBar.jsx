import React from 'react';

const SearchBar = () => (
    <div>
        <form >
            <label></label>
            <input type="text" placeholder="Have a question? search for answer..." />
            <button>Search</button>

        </form>
    </div>
)

export default SearchBar;